# Specs Index

Navigation aid only. Use this file to find the relevant spec, then open only that spec or section.

- `2026-05-13-rag-multi-instance-design.md`: AI documentation RAG multi-instance readiness with Elasticsearch vector store target.
- `2026-05-15-cms-comments-moderation-design.md`: CMS content comments with translation-aware moderation and optional AI voting.
- `2026-05-15-modification-moderation-design.md`: Generic Core moderation pipeline/event bus that supersedes comment-specific orchestration names.
- `2026-05-21-module-testing-strategy-design.md`: Module-owned test taxonomy and suite strategy.
- `2026-05-21-settings-group-cache-invalidation-design.md`: Core settings cache invalidation by group.
- `2026-05-25-erp-m36-m4-m6-m7-design.md`: ERP pending milestone alignment for M3.6, M4, M6, and M7.
- `2026-06-30-erp-hardening-bugs-money-math-design.md`: ERP v1 hardening — **implemented** P0/P1 fixes + post-review patch (`971851d` ERP, `15b11c8` Core).
- `2026-06-30-erp-hardening-spec2-filament-domain-actions-design.md`: ERP Point 0 master — mandatory non-API work and internal `/app` actions complete; external `/api/v1` deferred, Gantt/mobile optional, importers excluded and tracked separately.
- `2026-06-30-cms-graph-layer-design.md`: Core Graph Framework for CRUD-aligned graph expand/search/stats; CMS is the first consumer/provider, not the owner of the graph engine.
- `2026-07-02-cms-content-provenance-ai-assistance-design.md`: CMS content origin, references bibliography (`cms_contents_references`), and per-translation `ai_assistance` enum for EU AI Act Article 50 disclosure.
- `2026-07-09-mes-module-decisions-design.md`: MES locked product/architecture decisions (scope, numbering, backflush, ERP linkage).
- `2026-07-09-large-dataset-query-patterns-design.md`: Eloquent iteration rules (`get`/`lazy`/`chunk`/`cursor`), Core query/memory backlog, Filament table/widget performance checklist.
- `2026-07-09-database-guidelines-design.md`: Multi-DB compatibility (MySQL, MariaDB, PostgreSQL, Oracle, SQLite), migrations, indexes, FULLTEXT/Scout rules, `MigrateUtils` patterns.
- `2026-07-16-rag-retrieval-strategy-design.md`: Locked documentation RAG direction — vector baseline, evaluation-first hybrid/reranking roadmap, and evidence-gated optional graph retrieval.
- `2026-07-16-in-app-ai-assistance-security-design.md`: Mandatory developer/in-app profile isolation, separate corpora, fail-closed guardrails, and read-only ACL-preserving Core Graph tools.
- `2026-07-17-application-content-retrieval-design.md`: General Core provider contract for ACL-preserving module evidence retrieval, AI tool orchestration, CMS reference provider, and a separately gated public phase.
- `2026-08-04-documentation-rag-evaluation-baseline-design.md`: RAG goal R0 — per-module documentation retrieval evaluation baseline (dataset + deterministic retrieval-only harness mirroring application-content eval), Core/user first, regression gate, opt-in Level-2 generation scoring, and a forward-looking assistant-level (R1) evaluation contract.
- `2026-08-06-assistant-profile-scope-design.md`: RAG goal R1a — server-owned `AssistantScope` (moduleKey / dataAccess / docScope) resolved from profile + verified context; module-scopes the in-app documentation surface (`module OR cross_cutting_user` marker), gates app-data tools by dataAccess ("no module → docs only"), keeps CLI generic, seams a future superadmin profile. Security filters unchanged; R1b eval separate.
- `2026-07-21-aggregate-version-sets-design.md`: Generic Core version sets, synchronous single-connection history, composite pivot identity, aggregate restore, and the mandatory multi-connection recovery milestone.
- `2026-07-29-revision-centric-aggregate-history-design.md`: **Draft for adversarial review** — revision-first aggregate state snapshots with relation version vectors and optional atomic version sets; implementation remains paused.
- `2026-08-04-revision-centric-aggregate-history-review.md`: **Open review ledger** — seven Critical findings, candidate resolution paths, and the closure gate for the revision-centric aggregate-history draft.
- `2026-07-29-filament-environment-indicator-design.md`: Custom Filament env badge + App/module version dropdown (replaces pxlrbt).
- `2026-07-29-filament-make-resources-design.md`: Batch `filament:make-resources` + permanent Filament ClassGenerator rebind with Laraplate HasTable/HasRecords/HasForm trait resolution and `laraplate_owned` module gate.
- `2026-07-29-hasform-entity-preset-design.md`: Core `HasForm` Entity → Preset cascade for `HasDynamicContents` models; persist only `presettable_id`.
- `2026-07-30-filament-generate-trait-merge-design.md`: Pass Filament `--generate` columns/fields into HasTable/HasForm; strip trait-owned duplicates at generate + runtime (timestamps/validity composites).
- `2026-07-29-filament-toggle-inline-default-design.md`: Global Filament `Toggle::inline(false)` default (label above switch); form `inlineLabel()` unchanged.
- `2026-07-22-module-import-command-framework-design.md`: Core abstract import command infrastructure with module-owned CMS/ERP commands and gated Symfony ERP adapter.
- `2026-07-31-domain-action-http-routes-design.md`: **Implemented** internal `/app` domain actions — generic POST route, per-entity registry, generic-verb override guard, and ERP handlers.
- `2026-07-31-sao-module-design.md`: SAO (Simply Another Orchestrator) — correlation ledger (code ↔ errors ↔ work); deploy census; reliable silence; cross-project fingerprints; RC≠stable release history; deterministic ownership suggestions; evidence-based closure; phased roadmap.
- `2026-07-31-seeder-orchestration-design.md`: Production seeding — explicit dependency graph replacing `module.json` priority (fixes MES-before-ERP), single `SeedReconciler` with field-level drift semantics, computed `seeded_value` baseline, per-node atomicity with in-run resume, and cleanup keyed on module state. No process-level parallelism.
- `2026-08-01-sao-phase-1a-ticketing-core-design.md`: SAO phase 1a — internal ticketing core: projects, per-project ticket keys, shareable per-type workflow schemes with enforced transitions, global statuses with canonical categories, comments with human/system origin, history as a read model over Core versions, Laraplate-only authorization.
- `2026-08-15-sao-phase-3a-driver-framework-foundation-design.md`: **Implemented** — SAO phase 3a driver framework foundation: open driver registry, per-capability contracts (`issues`/`vcs`/`logs`/`releases`), `Connection` with encrypted-at-rest credentials + optional env override (F4), and a per-capability conformance suite proven offline by one in-memory reference driver. No concrete external driver (Redmine is 3b); deliberately built before Phase 2.
- `2026-08-15-core-media-foundation-design.md`: **Implemented** — promoted `spatie/laravel-medialibrary` ownership (the `vend_media` table, the media model, and the app-wide `media_model` config) from CMS to Core via a guarded migration, table name and data intact, so CMS and SAO both consume one Core media foundation. Unblocked SAO 1b attachments.
- `2026-08-15-sao-phase-1b-ticket-enrichment-design.md`: **Implemented (model/service level)** — SAO phase 1b ticket enrichment: project-scoped labels, watchers (record-only, no delivery), attachments via the Core media foundation, due dates with overdue scopes, typed ticket relations (blocks/duplicates/relates), and advanced search over the ACL-scoped visible query with saved filters. Offline, independent of the error flow. Optional Filament surfaces (Task 7) deferred.
- `2026-08-15-sao-phase-3b-issues-sync-design.md`: **Implemented (3b-core + Redmine)** — SAO phase 3b bindings and issues synchronization: `ProjectBinding` (project × connection × capability + sync direction + status/priority maps), a `BindingContext` enriching capability calls, `NormalizedIssue`, an idempotent direction-aware `IssueSyncService`, `TicketLink`, the network-free `internal` issues driver (reference implementation, built first), and five external issues drivers (Redmine, Jira, GitHub, GitLab, Bitbucket) each passing its conformance gate over an `Http::fake()`. Filament connection/binding surfaces (3b-ui) done; the driver set later widened to nine issues drivers (adding YouTrack, Azure DevOps, Linear). Both transports are now live: scheduled inbound **pull** (`sao:sync:issues` → `IssueSyncPoller`, paging the driver and reconciling each issue) and inbound **push** for logs connections (see phase 4). Live-instance verification against a real remote remains.
- `2026-08-16-sao-phase-1c-kanban-board-design.md`: **Implemented** — SAO phase 1c board: a `TicketBoardService` read model over the ACL-scoped visible query yielding status-ordered `BoardColumn`s, and a Filament board page moving cards only through `WorkflowService`'s allowed transitions. No new table, no new dependency; HTML5 drag-and-drop deferred (needs an approved kanban package).
- `2026-08-16-sao-phase-5-vcs-releases-design.md`: **Implemented** — SAO phase 5: the `vcs` and `releases` capabilities on the Git-host drivers (GitHub, GitLab, Bitbucket), each passing `VcsConformance` + `ReleasesConformance` over an `Http::fake()`. Issue trackers (Redmine, Jira) gain nothing; code-to-work correlation and version census are out of scope here.
- `2026-08-16-sao-phase-5b-code-to-work-releases-design.md`: **Implemented** — SAO phase 5b: the correlation data built on phase 5's reads. `ChangeRef` (a commit/PR/tag linked to a ticket with its producing source), `Release`/`ReleaseTag`/`TicketRelease` (a product version, its stable/candidate tags, and tickets attributed as promised/shipped independent of workflow status), and `Environment` + `DeployCensusService` (passive `observe`, active `recordProbe`, TTL staleness, and a `census(project, ttl)` read model). Offline; Filament surfaces for releases/environments and live probe transport are follow-ups.
- `2026-08-16-sao-phase-6-fix-propagation-closure-design.md`: **Implemented + extended** — SAO phase 6 core: `Signal.ticket_id` + `ChangeRef.merged_at`; `FixStatusResolver` ("already fixed on dev, deploy missing"); six pure closure conditions over a `ClosureContext` (a candidate never satisfies `fix_released`); project-scoped `ClosurePolicy` (`{key, config}` json + action, `propose` default); `ClosureEvaluator` (AND, empty set never satisfies); `ClosureAudit` + `ClosureAuditService` ("closed because" / reopen marks premature with "returned after"); `TimeToTruthService`. Follow-on slices also landed: `ClosureApplicationService` applies a satisfied policy to the ticket **through `WorkflowService`**; the ownership chain — `OwnershipSuggestion` + service (never auto-assigns, D14), `ContributorIdentity` directory + `ContributorIdentityMap`, the CODEOWNERS / recent-touch / blame (`BlameCapability`, GitHub-only) evidence resolvers, `OwnershipSuggestionCoordinator` (incl. `suggestForPullRequest` discovering changed files via `PullRequestChangedPathsResolver`); phase-8 phrasing (`SuggestionPhraser` — deterministic `TemplateSuggestionPhraser` + `AiSuggestionPhraser` over the optional-AI `SuggestionTextGenerator`/`EventTextGenerator` → Core `AiTextGenerationRequested` event); and Filament surfaces (release/environment/closure-policy CRUD, closure-audit/ownership-suggestion read-only). The live-model generator binding has since landed (AI text generation now targets a configurable model, `AI_TEXT_GENERATION_MODEL`); remaining: live PR-merge transport (the inbound logs webhook and scheduled issue polling are live — see phases 3b/4).
- `2026-08-16-sao-phase-2-signals-fingerprinting-design.md`: **Implemented (ingest/grouping level)** — SAO phase 2: Core-owned fingerprint normalization chain shared by the in-process resolver and SAO's payload path (line excluded from the hash, value-position-only numerics), project-scoped signals with a cross-project-comparable key and `algo_version` from the first migration, occurrences and aliases, and structurally-closed loop protection (pipeline-origin marker + per-group rate limit). Ticket closure (phase 6) landed separately; ticket **auto-open** from a signal is now live too (`SignalTicketOpener` + `sao:signals:auto-open`, idempotent by `Signal.ticket_id`, gated by `sao.signals.auto_open.*`). Recompute/alias rewrites remain out of scope.
- `2026-08-16-sao-phase-4-ingest-source-profiles-design.md`: **Implemented (ingest level)** — SAO phase 4: configurable source profiles (matchers + dot-path bindings, no new JSONPath dependency), a generic webhook ingest recording every delivery as an auditable `IngestEvent`, delivery-id idempotency, an ordered correlation ruleset that records the winning rule, and a pure dry-run replay. Concrete logs drivers (Graylog/Sentry and later GlitchTip/Rollbar/Bugsnag/Honeybadger/Grafana/Datadog/Elastic/BetterStack — ten in all) and the inbound push transport have since landed: `POST api/v1/webhooks/{connection}` (`DriverWebhookIngestService`) verifies the driver signature, unpacks the delivery, and ingests each event into the connection's `logs`-bound projects, deduped and audited as `IngestEvent`s. The Filament ingest surface is complete: a read-only `IngestEventResource` (list + view) surfaces the recorded deliveries and their outcomes, a writable `SourceProfileResource` (matchers repeater + field-bindings) authors the generic-ingest profiles, and `sao:ingest:replay` dry-runs an event against a profile.
- `2026-08-16-ai-live-text-generation-design.md`: **Implemented** — the optional live LLM path is production-ready behind `AI_TEXT_GENERATION_ENABLED`, contract and consumers unchanged: feature-flag gate + failure→unfulfilled fallback, cost controls (per-purpose rate limit, optional short-TTL cache keyed by purpose+prompt, output length cap on a word boundary; a hard wall-clock timeout is left to the provider's HTTP client and a client timeout is caught as a Throwable→unfulfilled), output sanitization, structured outcome logging, and a gated live smoke test (skipped unless `AI_LIVE_TESTS=1` + creds). Offline suite green (10 listener cases + skipped smoke). Extended since: an optional per-feature model binding (`AI_TEXT_GENERATION_MODEL`) lets ownership phrasing run on its own model independent of chat — `ProviderFactory`/`ChatAgent` gained an additive model override, proven offline by a `RecordingHttpClient` injected into a real provider (request carries the bound model, canned response parsed back — the model call minus the socket). Plan: `docs/superpowers/plans/2026-08-16-ai-live-text-generation.md`.
- `2026-08-23-sao-fix-resolution-attribution-design.md`: **Implemented (all 5 phases)** — SAO: how to know which commits/versions actually resolve a ticket, from evidence not a human flag. `ChangeRefRelation` (Fixes/Mentions), `TicketReferenceExtractor`/`CodeReferenceWriter`, `ReleaseAttributionService` (semver tag classifier), pull `sao:vcs:scan` + a `code` PR-merge webhook (generic + GitHub drivers), and `ClosureCoordinator`/`sao:closure:run` with a settings-gated auto-close (`SAO_CLOSURE_AUTO_CLOSE`). The read side is built (`ChangeRef`, `Release`/`ReleaseTag`/`TicketRelease`, deploy census, `FixStatusResolver`, evidence-based closure, `Resolved` deliberately non-terminal) but the write side is missing — only the `Deployment` feed is live. Proposes the attribution writers: `ChangeRef` from commit/PR message-parsing over the `vcs` capability + webhooks, and `Release`/`TicketRelease` from `ReleasesCapability::firstTagContaining`. Frames the external-dependence boundary (git facts) vs SAO's edge (the correlation ledger joining code↔signals↔work↔releases↔deploys as the connectors' selling point).
- `2026-08-23-sao-tracker-migration-importer-design.md`: **Proposed (draft)** — "switch to Laraplate, import your history (all or open-only)". Reuses the `issues` capability paging (`IssueSyncPoller` already walks all pages), `NormalizedIssue`, the idempotent `IssueSyncService::reconcile`, and `RecordOrigin`. Adds an async resumable bulk-import job, `all|open-only` filters, a `shadow → owned` cutover, and later comment/attachment history (capability extension) and error-service/file-dump backfill (shared with the bulk-import spec).
- `2026-08-23-core-bulk-import-mapping-design.md`: **Proposed (draft)** — generic bulk import: upload JSON/CSV/Excel/SQL → dropdown column mapping with a LibreOffice-Calc-like preview → confirm → queued batched job → in-app notification. Maps reusable pieces (Core `AbstractImportCommand`/`RecordOriginRegistry`, CMS mapper/pipeline, ERP `BankStatementCsvImporter` column-map precedent, `league/csv` + `openspout`, Filament's installed-but-unused Import stack) and the gaps (preview UI, persisted mapping, JSON/SQL parsing, SPA exposure, per-entity registration). Recommends a Core-owned `ImportSession`/`ImportProfile` feature reusable by SPA + Filament; SQL gated/deferred for security.
- `2026-08-23-core-in-app-notifications-design.md`: **Proposed (draft)** — in-app notification tray, push vs poll. Foundation: publish the `notifications` table + standardise the `database` channel. Transport: start **poll** (enable Filament's installed-but-off database-notifications feed + a SPA notifications API), keep **push** (Reverb/Echo `toBroadcast`) a later flagged transport swap on the same data model — no websockets in phase 1. Wires the latent producers (AI `ActionRequest` admin-intervention, modification reject-and-request-correction feedback, deadline reminders from `Ticket::dueWithin`/ERP schedules — the app currently schedules nothing), per-user `preferences` opt-in, and a notification-type catalog.
- `2026-08-06-crud-facet-counters-design.md`: **Implemented (tier 1 + tier 2)** — funnel double-counter (per-facet `total`/cross-filtered `count`) extracted from the parked Grid subsystem, on `CrudService` behind the dedicated `/crud/facets/{module}/{entity}` action (`CrudController::facets` + `FacetsRequest`). Tier 1 `facetCounts` (enumerable facets, per-option counting, `excludeFacetField` self-exclusion). Tier 2 `facetValues(FacetQuery): FacetPage` — one SQL `GROUP BY` on a key column with the facet's own pagination/search/sort, a `distinctValues` count, and the key≠label two-step (`resolveFacetLabels` over the page's keys — base columns or a single-hop `relation.column` via a `BelongsTo` keyed by `groupBy`, no join), including label search/sort (`labelField` + `FacetSort::Label*`) done without a join — `whereIn` on matching related keys for search, a correlated `ORDER BY` subquery for sort — so it stays portable and free of the ACL/soft-delete column collisions that originally deferred it. A singular `facet` payload selects tier 2. Covered by `CrudServiceFacetCountsTest` + `CrudServiceFacetValuesTest` + `CrudFacetsTest`. Deferred: multi-hop label paths only. No Grid routes/request layer revived.
