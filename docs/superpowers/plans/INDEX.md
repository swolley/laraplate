# Plans Index

Navigation aid only. Use this file to find the relevant plan, then open only that plan or section.

- `2026-05-13-rag-multi-instance-elasticsearch.md`: Implement Elasticsearch-backed RAG vector store.
- `2026-05-15-cms-comments-moderation.md`: Implement CMS comments with AI-assisted moderation.
- `2026-05-21-module-testing-strategy.md`: Reorganize module/application test suites.
- `2026-05-21-settings-group-cache-invalidation.md`: Implement group-level settings cache invalidation.
- `2026-05-25-erp-m36-purchase-invoice-posting.md`: Verify and complete purchase invoice posting cleanup.
- `2026-05-25-erp-m4-policies-filament-reporting.md`: ERP policies, Filament actions, and reporting alignment.
- `2026-05-25-erp-m61-bank-reconciliation.md`: ERP bank statement import and reconciliation.
- `2026-05-25-erp-m62-returns-management.md`: ERP customer/supplier returns with inventory effects.
- `2026-05-25-erp-m63-einvoice-sdi.md`: ERP e-invoice stub and submission structure.
- `2026-05-25-erp-m71-advanced-pricelists.md`: ERP advanced pricelists and discount rules.
- `docs/superpowers/plans/2026-06-19-mes-module-full-implementation.md`: Full MES module implementation plan.
- `docs/superpowers/specs/2026-07-09-mes-module-decisions-design.md`: MES locked decisions (confirmed 2026-07-09).
- `2026-06-23-erp-accounting-golden-master.md`: ERP accounting golden-master regression tests.
- `2026-06-30-erp-hardening-spec1.md`: ERP Spec 1 hardening — **completed** (8 tasks + patch `971851d` ERP / `15b11c8` Core). Open follow-ups → Spec 2 master backlog.
- `2026-06-30-erp-hardening-spec2-phase2a.md`: ERP Spec 2 Phase 2A — Filament domain actions + state-aware policies (**completed**, ERP `300f9ef`).
- `2026-06-30-erp-hardening-spec2-phase2b.md`: ERP Spec 2 Phase 2B — commercial/banking UX, returns automation, and reporting polish (**completed**; deferred ideas moved to the master backlog).
- `2026-06-30-erp-hardening-spec2-phase3-remaining.md`: ERP Point 0 history — mandatory non-API work and internal `/app` domain actions complete; external `/api/v1` deferred, Gantt/mobile optional, importers tracked separately.
- `2026-06-30-cms-graph-layer.md`: Core Graph Framework — CRUD-aligned expand/search/stats in Core, CMS as first provider, Phase 5 materialized edges gated by benchmarks and invalidation design.
- `2026-07-02-cms-content-provenance-ai-assistance.md`: CMS content origin, references bibliography, and per-translation `ai_assistance` enum.
- `2026-07-09-query-memory-filament-performance.md`: Core query batching (DatabaseEngine SQLite, licenses list, closure rebuild) and Filament widget/cache follow-ups.
- `2026-07-16-rag-retrieval-strategy.md`: Evaluation-first documentation RAG evolution from vector baseline to optional hybrid/reranking, with a separate authorization gate for any graph spike.
- `2026-07-16-in-app-ai-assistance-security.md`: Mandatory server-owned profiles, isolated user RAG, fail-closed guardrails, and ACL-preserving read-only Core Graph tools for in-app assistance.
- `2026-07-17-application-content-retrieval.md`: General Core provider registry, authenticated module evidence retrieval, CMS record-level baseline, AI tool integration, and separately gated public assistance.
- `2026-07-21-core-version-set-foundation.md`: Implement the generic Core sole writer, synchronous single-connection version sets, pivot descriptors, forward restore, approval integration, and set-level retention.
- `2026-07-21-cms-versioned-categories-pilot.md`: Prove versioned composite pivots on `Content::categories()` with one set per root and explicitly partial Content coverage.
- `2026-08-06-core-versioned-relation-membership.md`: Milestone-1 foundation of the aggregate membership vector — relation descriptors + reference-relation membership capture/read via `HasVersionedRelations` (owned/restore/uuid/adapters deferred).
- `2026-07-22-module-import-command-framework.md`: Completed Core abstract import mechanics and CMS command migration; ERP scope transferred to its dedicated plan.
- `2026-07-22-erp-external-source-importers.md`: Separate ERP importer workstream, excluded from ERP Point 0; planned adapters cover legacy Symfony SQL, SPLID Excel, and supported Tricount exports.
- `2026-07-29-filament-environment-indicator.md`: Replace pxlrbt env indicator with Core plugin + module version dropdown.
- `2026-07-29-filament-make-resources.md`: `filament:make-resources` + ownership helper + permanent Filament ClassGenerator rebind.
- `2026-07-29-hasform-entity-preset.md`: Core `HasForm` Entity → Preset cascade; persist `presettable_id` only.
- `2026-07-31-domain-action-http-routes.md`: **Completed** `3-04`, `3-01`, `3-06` — PermissionName, internal domain-action registry/route, ERP handlers, and HTTP behavior matrix.
- `2026-07-31-seeder-orchestration.md`: Core `Modules\Core\Seeding` — dependency-graph seeder ordering, `SeedDefinition`/`SeedReconciler` field-level reconciliation, per-node resume ledger, and module-state-aware settings cleanup (**completed**, 12 of 12 tasks — Task 11 AI/CMS/ERP/MES seeder migration done and verified 2026-08-14: all module seeders use `SeedDefinition->ownedBy()`, `FullSeedRunTest` green).
- `2026-07-31-sao-phase-0-scaffolding.md`: Bring `Modules/SAO` from the `nwidart` stub to full Laraplate module compliance — licence/metadata/tooling/test harness/release scripts matching Core/ERP/AI/CMS/MES, zero domain code (**completed** — module scaffolded, registered, and compliant).
- `2026-08-15-sao-phase-3a-driver-framework-foundation.md`: **Completed** — SAO driver framework foundation (slice 3a): capability/ingest enums, driver base + per-capability contracts, open `DriverRegistry`, `Connection` model with encrypted-at-rest credentials + optional env override (F4), `ConnectionCredentialResolver`, and a per-capability conformance suite proven offline by an in-memory reference driver (SAO suite green, 167 passed). No concrete external driver; built before Phase 2 by design. 3b adds the first concrete driver (Redmine) + ticket sync. Spec: `docs/superpowers/specs/2026-08-15-sao-phase-3a-driver-framework-foundation-design.md`.
- `2026-08-15-sao-phase-3b-issues-sync.md`: **Done — 3b-core + five external drivers**; 3b-ui (Filament Connection resource + project bindings) done — SAO bindings and issues synchronization (Phase 3b): `SyncDirection`/`NormalizedIssue`, `BindingContext` enriching capability calls, `Connection.config`, `ProjectBinding`, `TicketLink`, the network-free `internal` issues driver (reference, built first), an idempotent direction-aware `IssueSyncService`, and five external `issues` drivers — **Redmine, Jira, GitHub, GitLab, Bitbucket** — each passing the issues conformance suite over an `Http::fake()` (live-instance verification and push ingest are follow-ups). Spec: `docs/superpowers/specs/2026-08-15-sao-phase-3b-issues-sync-design.md`.
- `2026-08-16-sao-phase-1c-kanban-board.md`: **Completed** — per-project board: a `TicketBoardService` read model over `TicketQueryService::visible()` yielding status-ordered `BoardColumn`s, and a Filament board page moving cards only through `WorkflowService`'s allowed transitions. Dependency-free (no drag-drop library); SAO suite green (223 passed at slice close). Spec: `docs/superpowers/specs/2026-08-16-sao-phase-1c-kanban-board-design.md`.
- `2026-08-16-sao-phase-5-vcs-releases.md`: **Completed** — `vcs` + `releases` capabilities on the GitHub, GitLab and Bitbucket drivers: commits/compare/file-at-ref/open-PR and tags/first-tag-containing. Added the `VcsConformance` battery; each Git host passes issues + vcs + releases over an `Http::fake()`. Code-to-work extraction, version census and deploy tracking remain. Spec: `docs/superpowers/specs/2026-08-16-sao-phase-5-vcs-releases-design.md`.
- `2026-08-16-sao-phase-5b-code-to-work-releases.md`: **Completed** — the four tasks landed: `ChangeRefType` + `ChangeRef`; `ReleaseStatus`/`ReleaseTagKind`/`TicketReleaseState` + `Release`/`ReleaseTag`/`TicketRelease`; `Environment` + `DeployCensusService` (observe/probe/staleness/census); docs + parent bump. Full SAO suite green. Spec: `docs/superpowers/specs/2026-08-16-sao-phase-5b-code-to-work-releases-design.md`.
- `2026-08-16-sao-phase-6-fix-propagation-closure.md`: **Completed + extended** — the five planned tasks (persisted facts + `FixStatusResolver`; six closure conditions + registry; `ClosurePolicy` + `ClosureContextResolver`; `ClosureEvaluator` + `ClosureAudit` + premature-closure memory; `TimeToTruthService`), plus follow-on slices beyond the original plan: `ClosureApplicationService` (apply a policy via `WorkflowService`); the full ownership chain (`OwnershipSuggestion` + service, `ContributorIdentity`/`ContributorIdentityMap`, CODEOWNERS/recent-touch/blame resolvers with the optional `BlameCapability`, `OwnershipSuggestionCoordinator` + `suggestForPullRequest`/`PullRequestChangedPathsResolver`); phase-8 phrasing seam (`SuggestionPhraser`, `TemplateSuggestionPhraser`, `AiSuggestionPhraser`, `SuggestionTextGenerator`/`EventTextGenerator`); Filament CRUD (release/environment/closure-policy) and read-only (closure-audit/ownership-suggestion) surfaces. Full SAO suite green (393 passed). Spec: `docs/superpowers/specs/2026-08-16-sao-phase-6-fix-propagation-closure-design.md`.
- Optional-AI seam (cross-module): Core `AiTextGenerationRequested` event + AI `HandleAiTextGenerationListener` (opt-in `AI_TEXT_GENERATION_ENABLED`) let SAO request AI phrasing with no SAO↔AI dependency, mirroring `ModelRequiresIndexing`. Core + AI suites green for the touched pieces.
- `2026-08-16-ai-live-text-generation.md`: **Completed** — the four tasks landed: config + feature-flag/failure preflight; cost controls (rate limit, cache, output cap); output hardening + structured observability; gated live smoke test + docs. Reuses the AI provider stack; event contract and SAO consumers untouched. Offline AI suite green (skipped live smoke). Spec: `docs/superpowers/specs/2026-08-16-ai-live-text-generation-design.md`.
- `2026-08-16-sao-phase-2-signals-fingerprinting.md`: **Completed (ingest/grouping level)** — shared fingerprint chain in Core (rule classes + `Fingerprinter`, line excluded, value-position numerics), SAO `PayloadFrameResolver`, `Signal`/`SignalOccurrence`/`SignalAlias` models, `GroupKeyResolver` + `SignalIngestService`, and loop protection (`PipelineContext` + `InternalLogSource` + per-group rate limiter). Full Core+SAO suites green. Ticket auto-open (phase 6) and real webhook transport (phase 4) remain. Spec: `docs/superpowers/specs/2026-08-16-sao-phase-2-signals-fingerprinting-design.md`.
- `2026-08-16-sao-phase-4-ingest-source-profiles.md`: **Completed (ingest level)** — `SourceProfile` (matchers + dot-path field bindings), `IngestEvent` (auditable per-delivery outcome), `WebhookIngestService` (dedupe, profile selection, normalization, ordered `CorrelationRuleset`, signal hand-off) and a pure `IngestReplayService` dry-run. Graylog/Sentry `logs` drivers and the ingest UI remain. Spec: `docs/superpowers/specs/2026-08-16-sao-phase-4-ingest-source-profiles-design.md`.
- `2026-08-15-core-media-foundation.md`: **Completed** — re-homed media ownership (table `vend_media`, `Core\Models\Media`, app-wide `media_model` config) from CMS to Core with a guarded, data-safe migration; CMS is now a consumer via `HasMultimedia` over Core's `HasMedia`. Both tasks done (Core, then CMS); Core + CMS suites green (media change introduced no new failures). Spec: `docs/superpowers/specs/2026-08-15-core-media-foundation-design.md`.
- `2026-08-15-sao-phase-1b-ticket-enrichment.md`: **Completed (model/service level)** — SAO ticket enrichment (Phase 1b): due dates, project-scoped labels, watchers (record-only), attachments (Core media foundation), typed ticket relations, and advanced search + saved filters over the ACL-scoped visible query. Tasks 1–6 done and committed with tests; full SAO suite green (213 passed). Task 7 (optional Filament surfaces) deferred. Spec: `docs/superpowers/specs/2026-08-15-sao-phase-1b-ticket-enrichment-design.md`.
- `2026-08-01-sao-phase-1a-ticketing-core.md`: SAO internal ticketing core — projects with per-project ticket keys, global statuses with canonical categories, shareable per-type workflow schemes with enforced transitions, comments (human/system origin), timeline read model over Core versions, Laraplate-only authorization, and Filament surfaces (**completed** — models/enums/migrations/services and all Filament resources present, SAO suite green; integration layer is phase 1b and not started).
- `2026-08-04-documentation-rag-evaluation-baseline.md`: RAG goal R0 — per-module documentation retrieval evaluation harness mirroring the application-content one (case/dataset value objects, deterministic `$search`-seam fixtures, `DocumentationEvaluationService`, `ai:evaluate-documentation`), Core/user dataset + regression gate; Level-2 generation opt-in and the assistant-level (R1) contract are spec-only.
- `2026-08-06-assistant-profile-scope.md`: RAG goal R1a — server-owned `AssistantScope` + resolver, module clause (`module OR cross_cutting_user`) added to documentation retrieval on top of unchanged security filters, `dataAccess`-gated app-data tools in `respond()` ("no module → docs only"), deterministic fixture emulation, `cross_cutting_user` frontmatter marker + content tagging. Backward-compatible (null scope = today); R0 stays green.
