# Plans Index

Navigation aid only. Use this file to find the relevant plan, then open only that plan or section.

- `2026-05-13-rag-multi-instance-elasticsearch.md`: Implement Elasticsearch-backed RAG vector store.
- `2026-05-15-cms-comments-moderation.md` (**shipped 2026-09-11**, generalized): Implement CMS comments with AI-assisted moderation. Moderation is inherited from `HasApprovals`; the AI side became a module-agnostic `ModerationAdapterRegistry` in Core, so the plan's comments-specific log, event and service were never built. Unticked checkboxes are not outstanding work.
- `2026-05-21-module-testing-strategy.md` (**revised 2026-09-15**): Reorganize module/application test suites. The suite taxonomy shipped; the remaining work is centralizing the runner, the test dependencies and the quality toolchain in the application, since the module-local runner it originally designed cannot work.
- `2026-05-21-settings-group-cache-invalidation.md` (**shipped 2026-09-15**): Implement group-level settings cache invalidation.
- `2026-05-25-erp-m36-purchase-invoice-posting.md`: Verify and complete purchase invoice posting cleanup.
- `2026-05-25-erp-m4-policies-filament-reporting.md`: ERP policies, Filament actions, and reporting alignment.
- `2026-05-25-erp-m61-bank-reconciliation.md`: ERP bank statement import and reconciliation.
- `2026-05-25-erp-m62-returns-management.md`: ERP customer/supplier returns with inventory effects.
- `2026-05-25-erp-m63-einvoice-sdi.md`: ERP e-invoice stub and submission structure.
- `2026-05-25-erp-m71-advanced-pricelists.md`: ERP advanced pricelists and discount rules.
- `docs/superpowers/plans/2026-06-19-mes-module-full-implementation.md`: Full MES module implementation plan.
- `docs/superpowers/specs/2026-07-09-mes-module-decisions-design.md`: MES locked decisions (confirmed 2026-07-09).
- `2026-06-23-erp-accounting-golden-master.md`: ERP accounting golden-master regression tests.
- `2026-06-30-erp-hardening-spec1.md`: ERP Spec 1 hardening — **completed** (8 tasks + patch `971851d` ERP / `15b11c8` Core). Open follow-ups → Spec 2 master backlog.
- `2026-06-30-erp-hardening-spec2-phase2a.md`: ERP Spec 2 Phase 2A — Filament domain actions + state-aware policies (**completed**, ERP `300f9ef`).
- `2026-06-30-erp-hardening-spec2-phase2b.md`: ERP Spec 2 Phase 2B — commercial/banking UX, returns automation, and reporting polish (**completed**; deferred ideas moved to the master backlog).
- `2026-06-30-erp-hardening-spec2-phase3-remaining.md`: ERP Point 0 history — mandatory non-API work and internal `/app` domain actions complete; external `/api/v1` deferred, Gantt/mobile optional, importers tracked separately.
- `2026-06-30-cms-graph-layer.md`: Core Graph Framework — CRUD-aligned expand/search/stats in Core, CMS as first provider, Phase 5 materialized edges gated by benchmarks and invalidation design.
- `2026-07-02-cms-content-provenance-ai-assistance.md` (**shipped 2026-09-15**, provenance generalized): CMS content origin, references bibliography, and per-translation `ai_assistance` enum.
- `2026-07-09-query-memory-filament-performance.md`: Core query batching (DatabaseEngine SQLite, licenses list, closure rebuild) and Filament widget/cache follow-ups.
- `2026-07-14-portable-search-matching.md` (**shipped 2026-09-15**): Portable short-name, acronym, identifier and natural-language matching across Elasticsearch, Typesense, PostgreSQL and Oracle. Moved here from the stack repository on delivery.
- `2026-07-16-rag-retrieval-strategy.md`: Evaluation-first documentation RAG evolution from vector baseline to optional hybrid/reranking, with a separate authorization gate for any graph spike.
- `2026-07-16-in-app-ai-assistance-security.md`: Mandatory server-owned profiles, isolated user RAG, fail-closed guardrails, and ACL-preserving read-only Core Graph tools for in-app assistance.
- `2026-07-17-application-content-retrieval.md`: General Core provider registry, authenticated module evidence retrieval, CMS record-level baseline, AI tool integration, and separately gated public assistance.
- `2026-07-19-model-connection-affinity.md`: Make every database operation use the connection that owns its data across Core/ERP models, commands, widgets, seeders and migrations, so moving a model off the default connection cannot silently redirect related queries or transactions. Moved here from the stack repository: the subject names only backend files.
- `2026-07-21-core-version-set-foundation.md`: Implement the generic Core sole writer, synchronous single-connection version sets, pivot descriptors, forward restore, approval integration, and set-level retention.
- `2026-07-21-cms-versioned-categories-pilot.md`: Prove versioned composite pivots on `Content::categories()` with one set per root and explicitly partial Content coverage.
- `2026-08-06-core-versioned-relation-membership.md`: Milestone-1 foundation of the aggregate membership vector — relation descriptors + reference-relation membership capture/read via `HasVersionedRelations` (owned/restore/uuid/adapters deferred).
- `2026-08-06-crud-facet-counters.md` (**shipped, recorded 2026-09-18**): Funnel double counter re-homed as a thin `CrudService` utility — tier 1 enumerable `facetCounts`, tier 2 open `facetValues` with pagination/search/sort, single-hop relation facets and label sources, over the standalone `crud.facets` route. Retrospective closed plan for spec/plan parity.
- `2026-07-22-module-import-command-framework.md` (**shipped 2026-09-15**): Completed Core abstract import mechanics and CMS command migration; ERP scope transferred to its dedicated plan.
- `2026-07-22-erp-external-source-importers.md`: Separate ERP importer workstream, excluded from ERP Point 0; planned adapters cover legacy Symfony SQL, SPLID Excel, and supported Tricount exports.
- `2026-07-29-filament-environment-indicator.md`: Replace pxlrbt env indicator with Core plugin + module version dropdown.
- `2026-07-29-filament-make-resources.md`: `filament:make-resources` + ownership helper + permanent Filament ClassGenerator rebind.
- `2026-07-29-hasform-entity-preset.md` (**shipped 2026-09-15**): Core `HasForm` Entity → Preset cascade; persist `presettable_id` only.
- `2026-07-31-domain-action-http-routes.md`: **Completed** `3-04`, `3-01`, `3-06` — PermissionName, internal domain-action registry/route, ERP handlers, and HTTP behavior matrix.
- `2026-07-31-seeder-orchestration.md`: Core `Modules\Core\Seeding` — dependency-graph seeder ordering, `SeedDefinition`/`SeedReconciler` field-level reconciliation, per-node resume ledger, and module-state-aware settings cleanup (**completed**, 12 of 12 tasks — Task 11 AI/CMS/ERP/MES seeder migration done and verified 2026-08-14: all module seeders use `SeedDefinition->ownedBy()`, `FullSeedRunTest` green).
- `2026-07-31-sao-phase-0-scaffolding.md`: Bring `Modules/SAO` from the `nwidart` stub to full Laraplate module compliance — licence/metadata/tooling/test harness/release scripts matching Core/ERP/AI/CMS/MES, zero domain code (**completed** — module scaffolded, registered, and compliant).
- `2026-08-15-sao-phase-3a-driver-framework-foundation.md`: **Completed** — SAO driver framework foundation (slice 3a): capability/ingest enums, driver base + per-capability contracts, open `DriverRegistry`, `Connection` model with encrypted-at-rest credentials + optional env override (F4), `ConnectionCredentialResolver`, and a per-capability conformance suite proven offline by an in-memory reference driver (SAO suite green, 167 passed). No concrete external driver; built before Phase 2 by design. 3b adds the first concrete driver (Redmine) + ticket sync. Spec: `docs/superpowers/specs/2026-08-15-sao-phase-3a-driver-framework-foundation-design.md`.
- `2026-08-15-sao-phase-3b-issues-sync.md`: **Done — 3b-core + five external drivers**; 3b-ui (Filament Connection resource + project bindings) done — SAO bindings and issues synchronization (Phase 3b): `SyncDirection`/`NormalizedIssue`, `BindingContext` enriching capability calls, `Connection.config`, `ProjectBinding`, `TicketLink`, the network-free `internal` issues driver (reference, built first), an idempotent direction-aware `IssueSyncService`, and five external `issues` drivers — **Redmine, Jira, GitHub, GitLab, Bitbucket** — each passing the issues conformance suite over an `Http::fake()`. Widened later to nine issues drivers (YouTrack, Azure DevOps, Linear). Both transports now live: scheduled inbound **pull** (`sao:sync:issues` → `IssueSyncPoller`) and inbound **push** for logs connections (phase 4); live-instance verification against a real remote remains. Spec: `docs/superpowers/specs/2026-08-15-sao-phase-3b-issues-sync-design.md`.
- `2026-08-16-sao-phase-1c-kanban-board.md`: **Completed** — per-project board: a `TicketBoardService` read model over `TicketQueryService::visible()` yielding status-ordered `BoardColumn`s, and a Filament board page moving cards only through `WorkflowService`'s allowed transitions. Dependency-free (no drag-drop library); SAO suite green (223 passed at slice close). Spec: `docs/superpowers/specs/2026-08-16-sao-phase-1c-kanban-board-design.md`.
- `2026-08-16-sao-phase-5-vcs-releases.md`: **Completed** — `vcs` + `releases` capabilities on the GitHub, GitLab and Bitbucket drivers: commits/compare/file-at-ref/open-PR and tags/first-tag-containing. Added the `VcsConformance` battery; each Git host passes issues + vcs + releases over an `Http::fake()`. Code-to-work extraction, version census and deploy tracking remain. Spec: `docs/superpowers/specs/2026-08-16-sao-phase-5-vcs-releases-design.md`.
- `2026-08-16-sao-phase-5b-code-to-work-releases.md` (**shipped 2026-09-15**): **Completed** — the four tasks landed: `ChangeRefType` + `ChangeRef`; `ReleaseStatus`/`ReleaseTagKind`/`TicketReleaseState` + `Release`/`ReleaseTag`/`TicketRelease`; `Environment` + `DeployCensusService` (observe/probe/staleness/census); docs + parent bump. Full SAO suite green. Spec: `docs/superpowers/specs/2026-08-16-sao-phase-5b-code-to-work-releases-design.md`.
- `2026-08-16-sao-phase-6-fix-propagation-closure.md`: **Completed + extended** — the five planned tasks (persisted facts + `FixStatusResolver`; six closure conditions + registry; `ClosurePolicy` + `ClosureContextResolver`; `ClosureEvaluator` + `ClosureAudit` + premature-closure memory; `TimeToTruthService`), plus follow-on slices beyond the original plan: `ClosureApplicationService` (apply a policy via `WorkflowService`); the full ownership chain (`OwnershipSuggestion` + service, `ContributorIdentity`/`ContributorIdentityMap`, CODEOWNERS/recent-touch/blame resolvers with the optional `BlameCapability`, `OwnershipSuggestionCoordinator` + `suggestForPullRequest`/`PullRequestChangedPathsResolver`); phase-8 phrasing seam (`SuggestionPhraser`, `TemplateSuggestionPhraser`, `AiSuggestionPhraser`, `SuggestionTextGenerator`/`EventTextGenerator`); Filament CRUD (release/environment/closure-policy) and read-only (closure-audit/ownership-suggestion) surfaces. Full SAO suite green (393 passed). The follow-up live-model generator binding has since landed (see the AI live-text-generation plan); the inbound logs webhook and scheduled issue polling are live (phases 3b/4); live PR-merge transport remains. Spec: `docs/superpowers/specs/2026-08-16-sao-phase-6-fix-propagation-closure-design.md`.
- Optional-AI seam (cross-module): Core `AiTextGenerationRequested` event + AI `HandleAiTextGenerationListener` (opt-in `AI_TEXT_GENERATION_ENABLED`) let SAO request AI phrasing with no SAO↔AI dependency, mirroring `ModelRequiresIndexing`. Core + AI suites green for the touched pieces.
- `2026-08-16-ai-live-text-generation.md`: **Completed** — the four tasks landed: config + feature-flag/failure preflight; cost controls (rate limit, cache, output cap); output hardening + structured observability; gated live smoke test + docs. Reuses the AI provider stack; event contract and SAO consumers untouched. Offline AI suite green (skipped live smoke). Extended since: a configurable per-feature model binding (`AI_TEXT_GENERATION_MODEL`) via additive `ProviderFactory`/`ChatAgent` overrides, proven offline with a `RecordingHttpClient` in a real provider (bound model in the request, canned response parsed). Spec: `docs/superpowers/specs/2026-08-16-ai-live-text-generation-design.md`.
- `2026-08-16-sao-phase-2-signals-fingerprinting.md`: **Completed (ingest/grouping level)** — shared fingerprint chain in Core (rule classes + `Fingerprinter`, line excluded, value-position numerics), SAO `PayloadFrameResolver`, `Signal`/`SignalOccurrence`/`SignalAlias` models, `GroupKeyResolver` + `SignalIngestService`, and loop protection (`PipelineContext` + `InternalLogSource` + per-group rate limiter). Full Core+SAO suites green. Both later follow-ups have since landed: real webhook transport (phase 4) and ticket auto-open from a signal (`SignalTicketOpener` + `sao:signals:auto-open`). Spec: `docs/superpowers/specs/2026-08-16-sao-phase-2-signals-fingerprinting-design.md`.
- `2026-08-16-sao-phase-4-ingest-source-profiles.md`: **Completed (ingest level)** — `SourceProfile` (matchers + dot-path field bindings), `IngestEvent` (auditable per-delivery outcome), `WebhookIngestService` (dedupe, profile selection, normalization, ordered `CorrelationRuleset`, signal hand-off) and a pure `IngestReplayService` dry-run. The ten `logs` drivers and the inbound push transport have since landed — `POST api/v1/webhooks/{connection}` (`DriverWebhookIngestService`): driver-signature verify → unpack → ingest into the connection's `logs`-bound projects, deduped and audited. The Filament ingest surface is complete — read-only `IngestEventResource`, writable `SourceProfileResource`, and `sao:ingest:replay` for dry-runs. Spec: `docs/superpowers/specs/2026-08-16-sao-phase-4-ingest-source-profiles-design.md`.
- `2026-08-15-core-media-foundation.md`: **Completed** — re-homed media ownership (table `vend_media`, `Core\Models\Media`, app-wide `media_model` config) from CMS to Core with a guarded, data-safe migration; CMS is now a consumer via `HasMultimedia` over Core's `HasMedia`. Both tasks done (Core, then CMS); Core + CMS suites green (media change introduced no new failures). Spec: `docs/superpowers/specs/2026-08-15-core-media-foundation-design.md`.
- `2026-08-15-sao-phase-1b-ticket-enrichment.md` (**shipped 2026-09-15**, optional UI deferred): **Completed (model/service level)** — SAO ticket enrichment (Phase 1b): due dates, project-scoped labels, watchers (record-only), attachments (Core media foundation), typed ticket relations, and advanced search + saved filters over the ACL-scoped visible query. Tasks 1–6 done and committed with tests; full SAO suite green (213 passed). Task 7 (optional Filament surfaces) deferred. Spec: `docs/superpowers/specs/2026-08-15-sao-phase-1b-ticket-enrichment-design.md`.
- `2026-08-01-sao-phase-1a-ticketing-core.md`: SAO internal ticketing core — projects with per-project ticket keys, global statuses with canonical categories, shareable per-type workflow schemes with enforced transitions, comments (human/system origin), timeline read model over Core versions, Laraplate-only authorization, and Filament surfaces (**completed** — models/enums/migrations/services and all Filament resources present, SAO suite green; integration layer is phase 1b and not started).
- `2026-08-04-documentation-rag-evaluation-baseline.md`: RAG goal R0 — per-module documentation retrieval evaluation harness mirroring the application-content one (case/dataset value objects, deterministic `$search`-seam fixtures, `DocumentationEvaluationService`, `ai:evaluate-documentation`), Core/user dataset + regression gate; Level-2 generation opt-in and the assistant-level (R1) contract are spec-only.
- `2026-08-06-assistant-profile-scope.md`: RAG goal R1a — server-owned `AssistantScope` + resolver, module clause (`module OR cross_cutting_user`) added to documentation retrieval on top of unchanged security filters, `dataAccess`-gated app-data tools in `respond()` ("no module → docs only"), deterministic fixture emulation, `cross_cutting_user` frontmatter marker + content tagging. Backward-compatible (null scope = today); R0 stays green.
- `2026-08-29-assistant-end-to-end-evaluation.md`: RAG goal R1b — per-module assistant evaluation over the composed `respond()`: case/dataset value objects + scoring service (mirror R0), a scripted-router fixture that drives the real `respond()` with fake providers + a tool-invoking `completion` (no LLM), CMS dataset + deterministic baseline gate. Level-2 live mode + `ai:evaluate-assistant` command deferred.
- `2026-08-29-sao-application-content-provider.md`: RAG goal R2 — second application-content provider `sao.tickets`, mirroring the CMS one: make `Ticket` Core-searchable (denormalized project/type/status/assignee/reporter/watchers/labels), `SaoTicketEvidenceProjector`, `SaoApplicationContentRetrievalProvider` (engine ranks → ACL rehydration via `TicketQueryService::visible()` → safe projection, lexical fallback), SAO-side registration. No AI-module change; global tags deferred.
- `2026-08-27-octane-readiness.md`: **Complete** (Octane install deferred) — remove the state/concurrency hazards that make the app unsafe on long-lived workers, **without installing Octane** (adoption deferred by decision; steps kept in the plan's appendix, server choice FrankenPHP not Swoole). Seven tasks, all beneficial under plain FPM: deprecate the `app/crud/grid/*` surface and run grid actions in-process (the subprocess branch was untested and discarded in-place `ResponseBuilder` mutations; Funnels survive as Facets in `CrudService`), signal-free soft deadline in `ApplicationContentDeadlineExecutor`, permission caches from `static` to `once()` (`AuthorizationService`, `HasValidations`, `HasTable`), bound `HasClosureTable::$depth_cache`, `PerModelSettingResolver` singleton → `scoped()` + per-request `DatabaseConfigOverlay` middleware, Spatie `register_octane_reset_listener` → true, docs. Adds an architecture guardrail test against process-wide primitives in request-reachable code. Contains a **verified baseline** of what Octane already resets (locale, config, auth guard, session, array cache, `once()`, scoped bindings) so no task fixes a non-problem, plus a warm-cache list that must **not** be converted.
- `2026-09-02-record-locking-overhaul.md` (**shipped 2026-09-09**): Rebuild `HasLocks` into three usable modes (editorial lease, deliberate hold, ownerless freeze). Fixes the schema bug that made the user dimension unusable (`locked_user_id` declared `timestamp` on all 10 lockable tables, invisible to tests because they run on SQLite), stops lock writes from bumping `lock_version`, adds `locked_until` with lazily-evaluated expiry (cron is housekeeping only), splits the CRUD error mapping that answered a body-less 304 for failures needing a reason, and applies row-level ACL filters to write paths. Permissions: `update` implies the lease, `lock` freezes, `unlock` removes someone else's lock, default ACL `locked_until < @now`.
- `2026-09-15-measured-retrieval-tuning-l1.md`: L1 measured retrieval tuning — a committed, per-query-class ranking profile behind the `search_adaptive_tuning` setting, with L0 as the exact fallback and an offline tuner. Written for this repository from the start: the subject names only backend files.
- `2026-09-09-r3-retrieval-quality-baseline.md` (**completed**): Real IR metrics (precision@k, recall@k, nDCG@k) added to the application-content evaluation harness, with a committed deterministic ranking-quality baseline for `cms.contents` and `sao.tickets`. Moved here from the stack repository on delivery: the subject names only backend files.
- `2026-09-10-r3-phase2-per-strategy-breakdown.md` (**completed**): Per-strategy ranked lists (keyword/vector/hybrid/fused/reranked) surfaced from the ensemble plus a per-strategy retrieval-quality evaluator reusing the Phase 1 IR metrics; deterministic plumbing and tests, real ES measurement deferred. Moved here from the stack repository on delivery.
- `2026-07-29-filament-toggle-inline-default.md` (**retrospective, shipped 2026-08-14**): Toggle renders label above switch by default. Written from the code so its spec is not orphaned; no task checkboxes by design.
- `2026-08-23-core-bulk-import-mapping.md` (**retrospective, shipped 2026-08-23**): generic bulk import — readers, `ImportSession`, mapping/preview API, queue job, Filament surface.
- `2026-08-23-core-in-app-notifications.md` (**retrospective, shipped 2026-08-24**): in-app notification tray, database channel, module-scoped column, poll-based delivery.
- `2026-08-23-sao-fix-resolution-attribution.md` (**retrospective, shipped 2026-08-23**): evidence-based fix attribution and closure conditions.
- `2026-08-23-sao-tracker-migration-importer.md` (**retrospective, shipped 2026-08-23**): import an external tracker's history into SAO, idempotent by `TicketLink`.
- `2026-09-15-erp-factories-and-demo-dataset.md`: twelve ERP model factories and a deterministic demo dataset in `DevERPDatabaseSeeder` — ERP is the only module with neither, and its 131 tests rebuild the same fixtures by hand (172 `Company` creations across 85 files).
- `2026-09-15-release-tooling.md` (**shipped 2026-09-15**): release tooling. Records the delivered consolidation to one root `version.sh` (moved here from the module testing strategy plan, ex Tasks 4b/4c), then nine tasks: fix `cliff.toml` and the untagged changelog step first, regenerate the seven changelogs, then a dependency-free bash harness and a TDD rewrite of `scripts/version.sh` (arguments in any order, git-cliff inference, no `--amend`, exit `10`), interactive choices, `--all` with pointer consolidation, `--changelog-check`, removal of the unused hook machinery, Composer scripts and `docs/releasing.md`.
- `2026-09-12-es-multilingual-index-and-multimodel-embeddings.md` (**delivered 2026-09-15, code complete; cutover outstanding**): 15-task subagent-driven refactor of ES multilingual indexing + multi-model per-translation embeddings across Core/CMS/AI/SAO. Spec: `docs/superpowers/specs/2026-09-12-es-multilingual-index-and-multimodel-embeddings-design.md`. Documented in `Modules/Core/docs/rag/SEARCH_RETRIEVAL_PIPELINE.md` + `Modules/AI/docs/SEARCH_AND_TRANSLATION.md`. Moved here from the stack repository on delivery. Open follow-up: per-translation `search_text` on `content_translations` for body embedding + a translation-aware DB-driver keyword search.
- `2026-09-16-content-translation-search-text.md`: DB-maintained per-translation `search_text` on `cms_contents_translations` via per-driver triggers, so Content embeddings include the body (PHP strips HTML at read time). Spec: `docs/superpowers/specs/2026-09-16-content-translation-search-text-design.md`. Three tasks: column + per-driver triggers + backfill (cross-driver parity + raw-write regeneration tests), `Content::$embed` switch + PHP cleanup in `collectEmbedText`, module docs. Part B (Postgres FTS + translation-aware DatabaseEngine) separate.
- `2026-09-08-es-index-mapping-and-vector-enablement.md` (**delivered 2026-09-08; embedding-only scope superseded by 2026-09-12 Task 3**): living tracker that made ES vector/hybrid search work end-to-end — dimension alignment (384), `scout:index` model resolution, `ElasticsearchEngine::createIndex` applying the `embedding` dense_vector mapping via `ElasticsearchService`, resilient degradation (LLM/reranker down), and the RCA of the empty-mappings bug. Deliberately scoped to the embedding field only, keeping other fields on dynamic mapping to avoid the translator's invalid-ES output on object/relation fields.
- `2026-07-13-global-performance-optimization.md`: conservative cross-module (Core/ERP/CMS/AI/Filament) performance plan — every optimization gated by a regression/guardrail test + a before/after measurement, one narrow behavior at a time, per-subsystem commits.
- `2026-07-29-erp-external-cash-import-foundation.md` (**Complete 2026-08-03**): generic idempotent ERP import entry point + a source-neutral cash-movement ingestion service (income, expenses, partner contributions/withdrawals, allocations).
- `2026-07-29-nebula-cash-importer.md` (**Blocked** on the ERP cash-import foundation): import Nebula's legacy Symfony SQL cash history (income/expenses/partner contributions/withdrawals/allocations) into ERP.
- `2026-09-16-search-modes-and-strategy-resolution.md`: Search modes — measure first (gate), then `SearchMode` enum + `SearchStrategy` + `ISearchStrategyResolver` and a cheap Core resolver, `AdvancedSearchService` resolving per call instead of receiving components in its constructor, AI decorating with one resolver and dropping its four global overrides, quality-driven retries with `IntelligentSearchAction` retired, then the HTTP parameters with permission and per-user limiter, then docs. The load-bearing change is removing the overrides; everything else makes that removal safe.
- `2026-09-16-in-app-assistant-tools.md`: In-app assistant tools — settle the risk-classification question first, then an `approval_gated_tools` capability in the policy catalog, wiring in `contextualTools()`/`respond()` with pending requests in the message metadata, the in-app profile only (asserted, not assumed), then docs. Builds no approval machinery: it all exists, what was missing was a caller.
- `2026-09-16-persistent-user-memory.md`: Cross-session per-user memory — `ai_memory_facts` table + `MemoryFact` + `FactKind` (user-scoped, subject supersession, per-kind expiry), `UserMemoryService` (hash dedup/secret-filter/guest+impersonation+user-switch exclusion/standing+topical reads), classify facts at extraction, promote `ConversationSummary` facts at the summarize hook, relevance-ranked bounded injection into the assistant memory context, honest conversation forget + per-user switch, memory-management surface, config/erasure/docs. Table store; Core Graph is the upgrade path.
- `2026-09-16-laravel-13-upgrade.md`: In-place Laravel 12 to 13 upgrade of the application and all six modules, using `../laraplate13/composer.lock` as the reference dependency set. Module manifests first (merge-plugin ANDs them), one root solve, then the `PreventRequestForgery` rename (the web-group CSRF exclusion silently breaks otherwise), full suite against a Laravel 12 baseline, Pest 5 / PHPUnit 13 plus `pest-plugin-phpstan` as a separate step, Boost guidelines and docs, submodule pointers. Decision gate on the `mtrajano/laravel-swagger` downgrade.
- `2026-09-16-rag-query-analytics.md`: Append-only `{app}_rag_queries` ES index for answered documentation-RAG queries. Task 1 is a blocking privacy review; then config + index/mapping + `ai:create-rag-query-index`, a `RagQueryAnalyticsWriter` (HMAC `user_ref`, hashed/raw/off query text, guest+impersonation exclusion, fail-open), a queued `LogRagQueryJob` dispatched from the `DocumentationService` answer flow behind `ai.features.faq.query_logging.enabled`, retention prune + erasure by user hash, then docs. Write-only telemetry: never read on the request path, never feeds retrieval or tuning. Spec: `docs/superpowers/specs/2026-09-16-rag-query-analytics-design.md`.
- `2026-09-19-cms-content-extension-seam.md`: Generic CMS seam letting another module attach a richer domain model to a `Content` without CMS knowing it. Nullable `contents.extended_type` morph-alias column, `HidesExtendedContent` default global scope + opt-in `withExtended()`, a batched upcast (two queries per entity-scoped page, no N+1) resolving extenders from a dedicated `ContentExtenderRegistry`, extender-side trait (back-relation with scope removed, `$with`, temp holder, `save()` override), symmetric/orphan lifecycle with a cascade guard, and one shared `contents` index the extender enriches with a nested `extension` section (CMS-owned, mapping composed, scoped reindex). Built and proven against a test-only stub extender before the first consumer. Spec: `docs/superpowers/specs/2026-09-17-cms-content-extension-seam-design.md`.
- `2026-09-18-sao-affected-version-and-maturity-census.md`: SAO detected-version + version-maturity census, extending phase 5b. Task 1 vocabulary (`ReleaseTagKind` +`Alpha`/`Beta` + `precedence()`, `ReleaseStatus` + `Observed`, derived `Release::effectiveMaturity()`, observed hidden by default); Task 2 provenance (normalized log ingest + human "add new" both `firstOrCreate` an `observed` release, unnormalizable strings not promoted); Task 3 affected version at two granularities (`SignalOccurrence.affected_version`/`affected_release_id` + `TicketReleaseState::Affected` + `Ticket::affectedRelease`); Task 4 the stable-only resolution invariant in the attribution service (`shipped` requires a `stable` tag, `affected` accepts any maturity); Task 5 docs + parent bump. Maturity lives on the tag, `Release` is the census row dedup'd per `(project, version)`, you are affected by any maturity but resolve only on stable. Spec: `docs/superpowers/specs/2026-09-18-sao-affected-version-and-maturity-census-design.md`.
